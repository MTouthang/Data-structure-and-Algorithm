/**
 * forEach iterate through an item of array
 * - does not return any thing.
 * - logging element to console
 * - updating element in array
 * - transforming array into a new format
 *
 */

const numbers = [1, 2, 3, 4, 5];

numbers.forEach((num) => {
  console.log(num);
});
